// iniciar com letras maiúsculas, separar por virgula, como pegar dados de enum

enum Colors {
    Red = 1,
    Blue,
    Green
}

function showColor(color) {
    console.log(color)
}

showColor(Colors.Red)